def is_lucky_number(num):
    return all(digit in '47' for digit in str(num))

n = input()
count_lucky_digits = sum(1 for digit in n if digit in '47')

if is_lucky_number(count_lucky_digits):
    print("YES")
else:
    print("NO")

